[project]
name = "dict"
version = "0.2.0"
description = "Tray-resident voice-to-clipboard transcriber (Windows + macOS)"
requires-python = ">=3.10"
dependencies = [
  "faster-whisper>=1.0.3",
  "sounddevice>=0.4.6",
  "numpy>=1.26,<2.3",
  "pynput>=1.7",
  "PySide6>=6.6",
  "pyperclip>=1.9.0",
  "anthropic>=0.39",  # optional polish stage via Claude Haiku
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-mock>=3.12"]

[project.scripts]
dict = "dict.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["dict*"]

[tool.pytest.ini_options]
markers = ["slow: integration tests that load models or touch audio hardware"]
addopts = "-m 'not slow'"
